# 隐私与长期记忆说明

## 默认状态

长期口味记忆默认关闭。用户没有完成明确同意前，系统不会读取或保存长期偏好，也不会把偏好注入 Agent 上下文。

## 目的限定

长期记忆仅用于根据用户主动提供的饮食口味偏好定制化推荐菜谱，不用于广告、信用评估、用户画像或其他无关目的。

## 保存的数据

系统只设计保存以下固定类别：

- 辣度
- 口味
- 菜系
- 偏好食材
- 不喜欢或避免的食材
- 饮食风格
- 烹饪方式
- 餐食偏好

每条偏好包含匿名 `context_id`、类别、内容、来源和时间戳。

系统不应保存或推断：

- 姓名、电话、邮箱、账号等身份信息
- 精确位置或设备标识
- 疾病、过敏、健康状态
- 宗教、政治观点或其他敏感个人信息
- 与菜谱推荐无关的私密对话内容

如果用户主动输入敏感信息，系统不会把它当作口味偏好保存。生产环境还应结合所使用的模型服务条款，确认数据是否会被第三方用于训练或日志留存。

## 同意机制

- 用户必须在界面中勾选同意说明后主动开启。
- 同意版本号为 `2026-09-17-v1`。
- 同意版本更新后，旧版本同意自动失效，用户需要重新确认。
- 关闭长期记忆后，后续对话不会再注入长期偏好，也不会通过对话工具新增偏好。
- 关闭与删除是两个动作：撤回同意会停止处理，删除会清空已保存内容。

## 数据最小化

- 不要求注册姓名、邮箱或手机号。
- 使用随机生成的匿名 `context_id` 隔离用户数据。
- 只允许固定类别和最长 60 个字符的偏好内容。
- 不自动从照片中提取偏好。
- 只有用户明确表达“记住”“以后都……”等持久化意图时，Agent 才允许调用保存工具。
- 单次请求优先于长期偏好。

## 用户权利

用户可以在页面中：

- 查看全部长期偏好
- 主动添加偏好
- 逐条删除偏好
- 导出 JSON 数据副本
- 撤回同意
- 彻底删除同意记录和全部偏好

## 保留期限

长期偏好保存至用户主动删除。关闭长期记忆只停止后续处理，不自动删除已有记录；如用户要求彻底删除，应调用“彻底删除我的记忆”。

## 安全建议

生产部署应补充：

- 真正的用户登录与授权校验
- 传输层 TLS
- 数据库静态加密
- 访问审计与最小权限
- 数据备份的删除同步策略
- 模型供应商的数据保留和数据跨境评估
- 根据所在地区适用的个人信息保护法、GDPR 或相关法规进行合规评审

> 本文件描述的是本项目实现中的隐私控制措施，不构成法律意见。实际合规性取决于部署主体、用户所在地、模型供应商、存储位置和业务场景，正式上线前应咨询专业法律顾问。
